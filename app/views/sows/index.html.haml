- if current_member.has_role?(:review) and current_user.active_dashboard.to_sym == :review
  = render 'review_dashboard'
- elsif current_member.has_role?(:admin) and current_user.active_dashboard.to_sym == :admin 
  = render 'admin_dashboard'
- else
  = render 'dashboard'
