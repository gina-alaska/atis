<%= form_for(@mau) do |f| %>
  <% if @mau.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mau.errors.count, "error") %> prohibited this mau from being saved:</h2>

      <ul>
      <% @mau.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
